{% extends "layout.html.twig" %}
{% block contenu %}
    {% if inscription_confirm is defined %}
        Votre compte a bien été créé !
    {% endif %}
    <form method="post" action="{{ path('adulte.validFormlogin') }}">

        <div class="row">

            <fieldset>
                <legend>Se connecter :</legend>
                <label>Nom d'utilisateur :
                    <input name="username"  type="text"  size="18" value="" />
                </label>

                <label>Mot de passe :
                    <input name="password"  type="password"  size="18" value="" />
                </label>
                {% if app.session.get('erreur') is not empty %}
                    <small class="error">{{ app.session.get('erreur') }}</small>
                {% endif %}

                <input type="submit" name="seConnecter" value="Se connecter" />
                <a href="{{ path('adulte.add')}}"> S'inscrire </a>
	</fieldset>
</div>
</form>
    <br>
    <center><b>admin - admin<br>
        parent - parent</b></center>

{% endblock %}
