<nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area"> 
      <li class="name"> 
        <h1 class="shake"> <a href="{{ path("adulte.index") }}">EasyPéri</a></h1>
      </li> 
      <li class="toggle-topbar menu-icon">
        <a href="#"><span>Menu</span></a>
      </li> 
    </ul> 
    <section class="top-bar-section"> 
      <ul class="left">
          {% if app.session.get('role') == ('ROLE_ADMIN') or app.session.get('role') == ('ROLE_PARENT')  %}
            <li class="divider"></li>
            <li class="active"><a href="{{ path("enfant.show")  }}">Enfants</a></li>
          {% endif %}
          {% if app.session.get('role') == ('ROLE_ADMIN') %}
              <li><a href="{{ path("adulte.show")  }}">Adultes</a></li>
              <li><a href="{{ path("activite.show")  }}">Activites</a></li>
          {% endif %}
      </ul>
      <ul class="right">
        {% if app.session.get('username') is defined and app.session.get('logged') == 1 %}
          <li><a><img id="user-icon" class="shake" src="{{ asset("assets/img/user_icon.gif") }}"/><b>{{ app.session.get('username') }}</b></a></li>
          <li><a href="{{ path('adulte.logout')  }}">Se déconnecter</a></li>
        {% else %}
          <li><a href="{{ path('adulte.login')  }}">Se connecter</a></li>
        {% endif %}
      </ul>
    </section> 
</nav>